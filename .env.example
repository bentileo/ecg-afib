# Copy to .env and fill in. The app runs without these; only the
# prediction history in the dashboard is unavailable.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_TABLE=predictions
